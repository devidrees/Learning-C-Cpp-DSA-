{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch C Program",
            "type": "cppvsdbg",  // Microsoft Visual C++ debugger for Windows
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal",  // Use externalTerminal to run outside VS Code
            "preLaunchTask": "build C program"
        }
    ]
}
